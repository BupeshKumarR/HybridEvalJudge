# LLM Judge Auditor Web Application - Environment Configuration

# =============================================================================
# GENERAL SETTINGS
# =============================================================================

# Environment (development, staging, production)
ENVIRONMENT=development

# Build target for Docker (development, production)
BUILD_TARGET=development

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# PostgreSQL Database
POSTGRES_DB=llm_judge_auditor
POSTGRES_USER=llm_judge_user
POSTGRES_PASSWORD=changeme
POSTGRES_PORT=5432

# Database URL (used by backend)
DATABASE_URL=postgresql://llm_judge_user:changeme@postgres:5432/llm_judge_auditor

# =============================================================================
# REDIS CONFIGURATION
# =============================================================================

# Redis Cache
REDIS_PORT=6379
REDIS_URL=redis://redis:6379/0

# =============================================================================
# BACKEND CONFIGURATION
# =============================================================================

# Backend API Port
BACKEND_PORT=8000

# Security
SECRET_KEY=dev-secret-key-change-in-production-to-strong-random-value

# Logging
LOG_LEVEL=info

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:80

# Number of worker processes
WORKERS=4

# Backend command (for development)
BACKEND_COMMAND=uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

# =============================================================================
# FRONTEND CONFIGURATION
# =============================================================================

# Frontend Port
FRONTEND_PORT=3000
FRONTEND_INTERNAL_PORT=3000

# API URLs (used by React app)
REACT_APP_API_URL=http://localhost:8000
REACT_APP_WS_URL=http://localhost:8000

# Node environment
NODE_ENV=development

# Frontend command (for development)
FRONTEND_COMMAND=npm start

# =============================================================================
# NGINX CONFIGURATION
# =============================================================================

# Nginx Ports
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443

# =============================================================================
# DOCKER CONFIGURATION
# =============================================================================

# Volume mode (rw for development, ro for production)
VOLUME_MODE=rw

# =============================================================================
# MONITORING & OBSERVABILITY
# =============================================================================

# Sentry DSN (optional, for error tracking)
# SENTRY_DSN=

# =============================================================================
# PRODUCTION OVERRIDES
# =============================================================================
# For production, create a .env file and override these values:
# - Change ENVIRONMENT to "production"
# - Set strong SECRET_KEY
# - Use strong database passwords
# - Set proper CORS_ORIGINS
# - Configure SENTRY_DSN
# - Set BUILD_TARGET to "production"
# - Change VOLUME_MODE to "ro"
# =============================================================================
